## 2024-05-18 - [Security Headers]

**Learning:** Next.js allows adding HTTP headers in `next.config.ts`. Security headers like `Permissions-Policy` restrict access to browser APIs (camera, mic, location) reducing attack surface.

**Action:** Add strict security headers to `next.config.ts` where feasible.
